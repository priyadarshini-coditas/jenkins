pipeline {
    agent any
    stages {
        stage('Submit Stack') {
            steps {
            sh "aws cloudformation create-stack --stack-name react-cloudfront --template-body file://cloudformations3.yml --region 'ap-south-1'"
              }
             }
            }
            }
